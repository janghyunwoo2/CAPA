## 조사 결과 요약

현재 시스템의 코드, 테스트 결과, 시딩 데이터를 전부 분석하고 최신 Text-to-SQL 연구를 조사한 결과를 정리했습니다.

### 핵심 진단: **왜 시딩과 프롬프트만으로는 안 되는가?**

현재 시스템은 **"1-shot 생성 → 그대로 실행"** 구조입니다. LLM이 한 번에 맞는 SQL을 뽑아야만 성공하고, 틀리면 그대로 실패합니다. QA 예제를 아무리 늘려도:

1. 프롬프트 규칙은 **참고사항**일 뿐, LLM이 100% 따르지 않음
2. 실패 케이스별 QA 추가는 **전형적 과적합** — 일반화 불가

### 과적합 없이 정확도를 높이는 5대 전략

| 순위 | 전략                                                                   | 효과     | 구현 난이도 |
| ---- | ---------------------------------------------------------------------- | -------- | ----------- |
| 🥇   | **Self-Correction Loop** (SQL 실패 시 에러 피드백 → 재생성 3회) | +5~15%p  | 낮음        |
| 🥈   | **질문 패러프레이징** (같은 SQL에 다양한 표현 시딩)              | +10~20%p | 낮음        |
| 🥉   | **Self-Consistency** (SQL 후보 여러 개 생성 → 다수결)           | +3~8%p   | 중간        |
| 4    | **Schema Linking** (질문 키워드로 관련 컬럼만 선별)              | +5~10%p  | 중간        |
| 5    | **Execution Feedback** (실행 결과 검증 → 재생성)                | +5~10%p  | 높음        |

### 즉시 조치 (Quick Win)

| 항목                                | 내용                                          |
| ----------------------------------- | --------------------------------------------- |
| **temperature=0**             | SQL 생성 시 결정론적 출력으로 안정성 확보     |
| **system/user 프롬프트 분리** | 현재 규칙이 user 메시지에 섞여 있어 효율 낮음 |
| **네거티브 규칙 강화**        | "하지 마라" 규칙 추가 (DATE() 금지 등)        |
| **RAG n_results 조정**        | 너무 많은 검색 결과가 노이즈로 작용           |

### 가장 중요한 결론

> **"시딩을 더 하거나, 실패 케이스마다 QA를 추가하는 건 하지 마세요."**
>
> 대신 **Self-Correction Loop** (3~5일 구현)만 추가해도 즉각적인 효과가 있습니다. SOTA 논문들(DIN-SQL, MAGIC, CSC-SQL)의 공통점은 **"한 번에 완벽한 SQL을 만들려 하지 않고, 생성→검증→수정 사이클을 돌린다"**는 것입니다.

시딩과 프롬프트 엔지니어링 보완하는게 좋겠다 근데.. 틀린    

테스트 마다 다 조건 추가하면 특정 쿼리만 잘나오게 나오게      

되는거 아냐?  도대체 어떻게 구현하면 과적합되지 않고          

보편적으로 정확도를 높힐 수 있을까? 정보좀 찾아봐             



● Agent(Text-to-SQL 과적합 없는 정확도 향상 방법 조사)        

  ⎿  Done (24 tool uses · 52.0k tokens · 7m 32s)

  (ctrl+o to expand)



● 정리해드리겠습니다.

  핵심 답변: 과적합 없이 일반화하는 방법



  왜 "케이스별 예시 추가"가 과적합인가



  ❌ 나쁜 방법: "3월 14일 캠페인 CTR" 질문이 틀렸으니 그 쿼리 

  예시를 추가

  → 그 질문만 잘 나오고, "3월 13일 캠페인 CTR"이나 "어제      

  캠페인 CTR"은 여전히 틀림



  ✅ 좋은 방법: "캠페인별 GROUP BY + CTR 계산 패턴"을

  카테고리로 추가

  → 날짜가 바뀌어도, 필터 조건이 달라져도 패턴이 적용됨       



  3가지 방어선 (Defense in Depth)



  1. 프롬프트 강화 — 즉시 적용 가능



  ┌────────────────────────────────┬──────────────────────┐   

  │          추가할 내용           │         효과         │   

  ├────────────────────────────────┼──────────────────────┤   

  │ CoT Step 1에 "DDL에 실제       │ campaign_name 같은   │   

  │ 존재하는 컬럼인지 확인 후      │ hallucination 방지   │   

  │ 선택" 추가                     │                      │   

  ├────────────────────────────────┼──────────────────────┤   

  │ 테이블 선택 규칙 명시          │ 잘못된 테이블 선택   │   

  │ (_summary=집계, 원본=개별      │ 방지                 │   

  │ 이벤트)                        │                      │   

  ├────────────────────────────────┼──────────────────────┤   

  │ "DDL에 없는 컬럼 절대 사용     │ 일반적 hallucination │   

  │ 금지" 규칙                     │  방지                │   

  └────────────────────────────────┴──────────────────────┘   



  2. 시딩 전략 — 패턴 기반으로 재설계



  특정 쿼리 재현이 아니라 SQL 패턴 카테고리로 시딩:

  - GROUP BY + CTR 계산 패턴 (날짜 무관)

  - device_type/platform별 집계 패턴

  - ad_combined_log_summary 써야 하는 케이스 documentation    



  3. 실행 오류 Self-Correction — 단기 구현 권장



  생성된 SQL이 Athena에서 오류 나면 → 오류 메시지를 LLM에 다시

   주입 → 1회 재생성.

  현재 sql_validator.py가 있으니 여기에 연결하면 됩니다.      

  → 이게 구현되면 campaign_name 같은 오류는 자동

  자가수정됩니다.



  ---
  어떤 순서로 할까요?


  1. 즉시: sql_generator.yaml CoT + 테이블 선택 규칙 추가     

  (프롬프트만 수정)

  2. 단기: documentation 시딩에 "언제 어떤 테이블" 설명 추가  

  3. 중기: Athena 오류 시 Self-Correction 1회 재시도 구현     


  1번만 해도 T001의 campaign_name 문제는 잡힐 가능성이        


  높습니다. 진행할까요?