# 광고 분석 일간 쿼리 가이드 (기간조회 버전)

## 7. 실제 활용 예시 (Redash 기간 파라미터 활용)

### 7.1 기간 파라미터를 활용한 개선된 쿼리
Redash의 Date 파라미터 2개(시작일/종료일)를 사용하여 기간별 분석을 지원합니다.

#### 일간 KPI 요약 (기간조회 버전)
```sql
-- 전체 KPI 요약 - 기간별 집계
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date
),
daily_summary AS (
    SELECT 
        date_parse(concat(CAST(year AS VARCHAR), '-', 
                         LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                         LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d') as date,
        COUNT(*) as total_impressions,
        SUM(CASE WHEN is_click THEN 1 ELSE 0 END) as total_clicks,
        SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as total_cost,
        SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) as total_revenue,
        SUM(CASE WHEN is_conversion THEN 1 ELSE 0 END) as total_conversions,
        COUNT(DISTINCT advertiser_id) as active_advertisers,
        COUNT(DISTINCT campaign_id) as active_campaigns,
        COUNT(DISTINCT ad_id) as active_ads
    FROM ad_combined_log_summary
    CROSS JOIN date_params
    WHERE 
        date_parse(concat(CAST(year AS VARCHAR), '-', 
                         LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                         LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
        BETWEEN start_date AND end_date
    GROUP BY 1
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    date_format(date, '%Y-%m-%d') as "날짜",
    total_impressions as "총_노출수",
    total_clicks as "총_클릭수",
    total_cost as "총_광고비",
    total_revenue as "총_매출",
    total_conversions as "총_전환수",
    active_advertisers as "활성_광고주수",
    active_campaigns as "활성_캠페인수",
    active_ads as "활성_광고수",
    CASE WHEN total_impressions > 0 
         THEN ROUND(CAST(total_clicks AS DOUBLE) / total_impressions * 100, 2)
         ELSE 0 END as "전체_CTR",
    CASE WHEN total_clicks > 0 
         THEN ROUND(total_cost / total_clicks, 0)
         ELSE 0 END as "전체_CPC",
    CASE WHEN total_clicks > 0 
         THEN ROUND(CAST(total_conversions AS DOUBLE) / total_clicks * 100, 2)
         ELSE 0 END as "전체_CVR",
    CASE WHEN total_cost > 0 
         THEN ROUND(total_revenue / total_cost, 2)
         ELSE 0 END as "전체_ROAS"
FROM daily_summary
CROSS JOIN date_params
ORDER BY date
```

#### 시간별 트렌드 분석 (기간조회 버전)
```sql
-- 시간별 노출수와 클릭수 추이 - 기간별 집계
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    CAST(hour AS INTEGER) as "시간",
    COUNT(*) as "노출수",
    SUM(CASE WHEN is_click THEN 1 ELSE 0 END) as "클릭수",
    SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as "광고비",
    SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) as "매출",
    COUNT(DISTINCT advertiser_id) as "활성_광고주수",
    AVG(COUNT(*)) OVER (PARTITION BY hour) as "평균_노출수",
    AVG(SUM(CASE WHEN is_click THEN 1 ELSE 0 END)) OVER (PARTITION BY hour) as "평균_클릭수"
FROM ad_combined_log
CROSS JOIN date_params
WHERE 
    date_parse(concat(CAST(year AS VARCHAR), '-', 
                     LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                     LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
    BETWEEN start_date AND end_date
GROUP BY hour, date_params.start_date, date_params.end_date
ORDER BY CAST(hour AS INTEGER)
```

#### 시간대별 ROAS 패턴 분석 (기간조회 버전)
```sql
-- 시간대별 ROAS 패턴 분석 - 시간별 광고 효율성 추이 (기간 평균)
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    CAST(hour AS INTEGER) as "시간",
    COUNT(DISTINCT date_parse(concat(CAST(year AS VARCHAR), '-', 
                                    LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                                    LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')) as "활동일수",
    COUNT(DISTINCT advertiser_id) as "활성_광고주수",
    COUNT(*) as "노출수",
    SUM(CASE WHEN is_click THEN 1 ELSE 0 END) as "클릭수",
    SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as "광고비",
    SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) as "매출",
    CASE WHEN COUNT(*) > 0 
         THEN ROUND(CAST(SUM(CASE WHEN is_click THEN 1 ELSE 0 END) AS DOUBLE) / COUNT(*) * 100, 2)
         ELSE 0 END as "CTR",
    CASE WHEN (SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END)) > 0 
         THEN ROUND(SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) / 
                   (SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END)), 2)
         ELSE 0 END as "시간별_ROAS"
FROM ad_combined_log
CROSS JOIN date_params
WHERE 
    date_parse(concat(CAST(year AS VARCHAR), '-', 
                     LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                     LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
    BETWEEN start_date AND end_date
GROUP BY hour, date_params.start_date, date_params.end_date
ORDER BY CAST(hour AS INTEGER)
```

#### 광고주별 일간 성과 분석 (기간조회 버전)
```sql
-- 광고주별 기간 성과 TOP 20
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    CONCAT(SUBSTR(advertiser_id, 1, 4), '..', SUBSTR(advertiser_id, -4)) as "광고주ID",
    advertiser_name as "광고주명",
    COUNT(DISTINCT date_parse(concat(CAST(year AS VARCHAR), '-', 
                                    LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                                    LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')) as "활동일수",
    COUNT(*) as "노출수",
    SUM(CASE WHEN is_click THEN 1 ELSE 0 END) as "클릭수",
    SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as "광고비",
    SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) as "매출",
    SUM(CASE WHEN is_conversion THEN 1 ELSE 0 END) as "전환수",
    CASE WHEN COUNT(*) > 0 
         THEN ROUND(CAST(SUM(CASE WHEN is_click THEN 1 ELSE 0 END) AS DOUBLE) / COUNT(*) * 100, 2)
         ELSE 0 END as "CTR",
    CASE WHEN SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) > 0 
         THEN ROUND(SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) / 
                   (SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END)), 2)
         ELSE 0 END as "ROAS"
FROM ad_combined_log_summary
CROSS JOIN date_params
WHERE 
    date_parse(concat(CAST(year AS VARCHAR), '-', 
                     LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                     LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
    BETWEEN start_date AND end_date
GROUP BY advertiser_id, advertiser_name, date_params.start_date, date_params.end_date
ORDER BY "매출" DESC
LIMIT 20
```

#### 캠페인별 성과 TOP 10 (기간조회 버전)
```sql
-- 캠페인별 기간 성과 TOP 10 - 가장 효율적인 캠페인 파악
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    CONCAT(SUBSTR(campaign_id, 1, 4), '..', SUBSTR(campaign_id, -4)) as "캠페인ID",
    campaign_name as "캠페인명",
    CONCAT(SUBSTR(advertiser_id, 1, 4), '..', SUBSTR(advertiser_id, -4)) as "광고주ID",
    COUNT(DISTINCT date_parse(concat(CAST(year AS VARCHAR), '-', 
                                    LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                                    LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')) as "활동일수",
    COUNT(*) as "노출수",
    SUM(CASE WHEN is_click THEN 1 ELSE 0 END) as "클릭수",
    SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as "광고비",
    SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) as "매출",
    SUM(CASE WHEN is_conversion THEN 1 ELSE 0 END) as "전환수",
    CASE WHEN SUM(CASE WHEN is_click THEN 1 ELSE 0 END) > 0 
         THEN ROUND(CAST(SUM(CASE WHEN is_conversion THEN 1 ELSE 0 END) AS DOUBLE) / 
                    SUM(CASE WHEN is_click THEN 1 ELSE 0 END) * 100, 2)
         ELSE 0 END as "CVR",
    CASE WHEN SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) > 0 
         THEN ROUND(SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) / 
                   (SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END)), 2)
         ELSE 0 END as "ROAS"
FROM ad_combined_log_summary
CROSS JOIN date_params
WHERE 
    date_parse(concat(CAST(year AS VARCHAR), '-', 
                     LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                     LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
    BETWEEN start_date AND end_date
GROUP BY campaign_id, campaign_name, advertiser_id, date_params.start_date, date_params.end_date
HAVING SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) > 100000  -- 최소 광고비 조건
ORDER BY "ROAS" DESC, "매출" DESC
LIMIT 10
```

#### 디바이스별 성과 분석 (기간조회 버전)
```sql
-- 디바이스별 기간 성과 분석 - 모바일 vs PC 비교
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    device_type as "디바이스",
    COUNT(*) as "노출수",
    SUM(CASE WHEN is_click THEN 1 ELSE 0 END) as "클릭수",
    SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as "광고비",
    SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) as "매출",
    SUM(CASE WHEN is_conversion THEN 1 ELSE 0 END) as "전환수",
    ROUND(COUNT(*) * 100.0 / SUM(COUNT(*)) OVER(), 1) as "노출_비중%",
    CASE WHEN COUNT(*) > 0 
         THEN ROUND(CAST(SUM(CASE WHEN is_click THEN 1 ELSE 0 END) AS DOUBLE) / COUNT(*) * 100, 2)
         ELSE 0 END as "CTR",
    CASE WHEN SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) > 0 
         THEN ROUND(SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) / 
                   (SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END)), 2)
         ELSE 0 END as "ROAS"
FROM ad_combined_log_summary
CROSS JOIN date_params
WHERE 
    date_parse(concat(CAST(year AS VARCHAR), '-', 
                     LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                     LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
    BETWEEN start_date AND end_date
GROUP BY device_type, date_params.start_date, date_params.end_date
ORDER BY "노출수" DESC
```

#### 시간대별 성과 비교 (기간조회 버전)
```sql
-- 시간대별 성과 비교 - 오전/오후/저녁/심야 구분 (기간 평균)
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date
),
hourly_data AS (
    SELECT 
        hour,
        CASE 
            WHEN hour BETWEEN 0 AND 5 THEN '심야(00-06시)'
            WHEN hour BETWEEN 6 AND 11 THEN '오전(06-12시)'
            WHEN hour BETWEEN 12 AND 17 THEN '오후(12-18시)'
            ELSE '저녁(18-24시)'
        END as time_zone,
        COUNT(*) as impressions,
        SUM(CASE WHEN is_click THEN 1 ELSE 0 END) as clicks,
        SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as cost,
        SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) as revenue,
        SUM(CASE WHEN is_conversion THEN 1 ELSE 0 END) as conversions
    FROM ad_combined_log
    CROSS JOIN date_params
    WHERE 
        date_parse(concat(CAST(year AS VARCHAR), '-', 
                         LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                         LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
        BETWEEN start_date AND end_date
    GROUP BY hour
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    time_zone as "시간대",
    SUM(impressions) as "노출수",
    SUM(clicks) as "클릭수",
    SUM(cost) as "광고비",
    SUM(revenue) as "매출",
    SUM(conversions) as "전환수",
    ROUND(SUM(impressions) * 100.0 / SUM(SUM(impressions)) OVER(), 1) as "노출_비중%",
    CASE WHEN SUM(impressions) > 0 
         THEN ROUND(CAST(SUM(clicks) AS DOUBLE) / SUM(impressions) * 100, 2)
         ELSE 0 END as "CTR",
    CASE WHEN SUM(cost) > 0 
         THEN ROUND(SUM(revenue) / SUM(cost), 2)
         ELSE 0 END as "ROAS"
FROM hourly_data
CROSS JOIN date_params
GROUP BY time_zone, date_params.start_date, date_params.end_date
ORDER BY 
    CASE time_zone
        WHEN '심야(00-06시)' THEN 1
        WHEN '오전(06-12시)' THEN 2
        WHEN '오후(12-18시)' THEN 3
        WHEN '저녁(18-24시)' THEN 4
    END
```

#### 일간 목표 달성률 분석 (기간조회 버전)
```sql
-- 기간 총 목표 대비 실적 달성률
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date,
        DATE_DIFF('day', DATE('{{ start_date }}'), DATE('{{ end_date }}')) + 1 as days_count
),
period_actual AS (
    SELECT 
        SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) as actual_revenue,
        SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as actual_cost,
        SUM(CASE WHEN is_conversion THEN 1 ELSE 0 END) as actual_conversions,
        COUNT(*) as actual_impressions,
        SUM(CASE WHEN is_click THEN 1 ELSE 0 END) as actual_clicks
    FROM ad_combined_log_summary
    CROSS JOIN date_params
    WHERE 
        date_parse(concat(CAST(year AS VARCHAR), '-', 
                         LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                         LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
        BETWEEN start_date AND end_date
),
targets AS (
    SELECT 
        {{ daily_revenue_target|100000000 }} * days_count as target_revenue,  -- 일일 목표 × 일수
        {{ daily_cost_target|50000000 }} * days_count as target_cost,        
        {{ daily_conversion_target|10000 }} * days_count as target_conversions
    FROM date_params
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    days_count as "기간_일수",
    actual_revenue as "실적_매출",
    target_revenue as "목표_매출",
    ROUND(actual_revenue / target_revenue * 100, 1) as "매출_달성률%",
    actual_cost as "실적_광고비",
    target_cost as "목표_광고비",
    ROUND(actual_cost / target_cost * 100, 1) as "광고비_집행률%",
    actual_conversions as "실적_전환수",
    target_conversions as "목표_전환수",
    ROUND(actual_conversions / target_conversions * 100, 1) as "전환_달성률%",
    CASE WHEN actual_cost > 0 
         THEN ROUND(actual_revenue / actual_cost, 2)
         ELSE 0 END as "ROAS",
    actual_impressions as "노출수",
    actual_clicks as "클릭수"
FROM period_actual
CROSS JOIN targets
CROSS JOIN date_params
```

#### 광고 소재별 성과 분석 (기간조회 버전)
```sql
-- 광고 소재별 기간 성과 TOP 20 - 가장 효과적인 광고 파악
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    ad_id as "광고ID",
    ad_name as "광고명",
    CONCAT(SUBSTR(campaign_id, 1, 4), '..', SUBSTR(campaign_id, -4)) as "캠페인ID",
    COUNT(DISTINCT date_parse(concat(CAST(year AS VARCHAR), '-', 
                                    LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                                    LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')) as "활동일수",
    COUNT(*) as "노출수",
    SUM(CASE WHEN is_click THEN 1 ELSE 0 END) as "클릭수",
    SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as "광고비",
    SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) as "매출",
    SUM(CASE WHEN is_conversion THEN 1 ELSE 0 END) as "전환수",
    CASE WHEN COUNT(*) > 0 
         THEN ROUND(CAST(SUM(CASE WHEN is_click THEN 1 ELSE 0 END) AS DOUBLE) / COUNT(*) * 100, 2)
         ELSE 0 END as "CTR",
    CASE WHEN SUM(CASE WHEN is_click THEN 1 ELSE 0 END) > 0 
         THEN ROUND(CAST(SUM(CASE WHEN is_conversion THEN 1 ELSE 0 END) AS DOUBLE) / 
                    SUM(CASE WHEN is_click THEN 1 ELSE 0 END) * 100, 2)
         ELSE 0 END as "CVR"
FROM ad_combined_log_summary
CROSS JOIN date_params
WHERE 
    date_parse(concat(CAST(year AS VARCHAR), '-', 
                     LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                     LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
    BETWEEN start_date AND end_date
GROUP BY ad_id, ad_name, campaign_id, date_params.start_date, date_params.end_date
HAVING COUNT(*) >= 100  -- 최소 노출수 조건
ORDER BY "CVR" DESC, "CTR" DESC, "매출" DESC
LIMIT 20
```

#### 지역별 성과 분석 (기간조회 버전)
```sql
-- 지역별 기간 성과 분석 - 지역별 광고 효율성 비교
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    region as "지역",
    COUNT(*) as "노출수",
    SUM(CASE WHEN is_click THEN 1 ELSE 0 END) as "클릭수",
    SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as "광고비",
    SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) as "매출",
    SUM(CASE WHEN is_conversion THEN 1 ELSE 0 END) as "전환수",
    ROUND(COUNT(*) * 100.0 / SUM(COUNT(*)) OVER(), 1) as "노출_비중%",
    ROUND((SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) * 100.0) / 
          NULLIF(SUM(SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END)) OVER(), 0), 1) as "매출_비중%",
    CASE WHEN COUNT(*) > 0 
         THEN ROUND(CAST(SUM(CASE WHEN is_click THEN 1 ELSE 0 END) AS DOUBLE) / COUNT(*) * 100, 2)
         ELSE 0 END as "CTR",
    CASE WHEN SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) > 0 
         THEN ROUND(SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) / 
                   (SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END)), 2)
         ELSE 0 END as "ROAS"
FROM ad_combined_log_summary
CROSS JOIN date_params
WHERE 
    date_parse(concat(CAST(year AS VARCHAR), '-', 
                     LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                     LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
    BETWEEN start_date AND end_date
GROUP BY region, date_params.start_date, date_params.end_date
ORDER BY "매출" DESC
```

### 7.2 Redash 파라미터 설정 방법
1. 쿼리 편집 화면에서 `{{ start_date }}`, `{{ end_date }}` 파라미터 자동 생성됨
2. 두 파라미터 타입을 모두 "Date"로 설정
3. 기본값 설정:
   - start_date: "7 days ago" 또는 "month start" 선택
   - end_date: "yesterday" 또는 "today" 선택

### 7.3 장점
- **기간 유연성**: 원하는 기간을 자유롭게 선택하여 분석 가능 (1일, 1주일, 1개월, 분기 등)
- **비교 분석**: 서로 다른 기간의 데이터를 쉽게 비교 가능
- **대시보드 활용**: 다양한 기간의 데이터를 하나의 대시보드에서 관리
- **성과 추적**: 캠페인 기간, 이벤트 기간 등 특정 기간의 성과 측정 용이

### 7.4 추가 실무 활용 예시

#### 기간별 일별 추이 분석
```sql
-- 기간 내 일별 주요 지표 추이
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date
),
daily_metrics AS (
    SELECT 
        date_parse(concat(CAST(year AS VARCHAR), '-', 
                         LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                         LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d') as date,
        COUNT(*) as impressions,
        SUM(CASE WHEN is_click THEN 1 ELSE 0 END) as clicks,
        SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as cost,
        SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) as revenue,
        SUM(CASE WHEN is_conversion THEN 1 ELSE 0 END) as conversions
    FROM ad_combined_log_summary
    CROSS JOIN date_params
    WHERE 
        date_parse(concat(CAST(year AS VARCHAR), '-', 
                         LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                         LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
        BETWEEN start_date AND end_date
    GROUP BY 1
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    date_format(date, '%Y-%m-%d') as "날짜",
    day_of_week(date) as "요일",
    impressions as "노출수",
    clicks as "클릭수",
    cost as "광고비",
    revenue as "매출",
    conversions as "전환수",
    CASE WHEN impressions > 0 
         THEN ROUND(CAST(clicks AS DOUBLE) / impressions * 100, 2) 
         ELSE 0 END as "CTR",
    CASE WHEN cost > 0 
         THEN ROUND(revenue / cost, 2) 
         ELSE 0 END as "ROAS"
FROM daily_metrics
CROSS JOIN date_params
ORDER BY date
```

#### 비용 효율성 분석 (기간 평균)
```sql
-- 기간 평균 비용 효율성 지표 분석 - CPC, CPM, CPA 추이
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date
),
period_metrics AS (
    SELECT 
        COUNT(*) as total_impressions,
        SUM(CASE WHEN is_click THEN 1 ELSE 0 END) as total_clicks,
        SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as total_cost,
        SUM(CASE WHEN is_conversion THEN 1 ELSE 0 END) as total_conversions,
        COUNT(DISTINCT date_parse(concat(CAST(year AS VARCHAR), '-', 
                                        LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                                        LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')) as days_count
    FROM ad_combined_log_summary
    CROSS JOIN date_params
    WHERE 
        date_parse(concat(CAST(year AS VARCHAR), '-', 
                         LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                         LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
        BETWEEN start_date AND end_date
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    days_count as "기간_일수",
    total_impressions as "총_노출수",
    total_clicks as "총_클릭수",
    total_conversions as "총_전환수",
    total_cost as "총_광고비",
    ROUND(total_impressions / days_count, 0) as "일평균_노출수",
    ROUND(total_clicks / days_count, 0) as "일평균_클릭수",
    CASE WHEN total_impressions >= 1000 
         THEN ROUND(total_cost / (total_impressions / 1000.0), 0)
         ELSE 0 END as "평균_CPM",
    CASE WHEN total_clicks > 0 
         THEN ROUND(total_cost / total_clicks, 0)
         ELSE 0 END as "평균_CPC",
    CASE WHEN total_conversions > 0 
         THEN ROUND(total_cost / total_conversions, 0)
         ELSE 0 END as "평균_CPA"
FROM period_metrics
CROSS JOIN date_params
```

#### 키워드별 성과 분석 (기간조회 버전)
```sql
-- 키워드별 기간 성과 TOP 30 - 가장 효과적인 키워드 파악
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    keyword as "키워드",
    COUNT(*) as "노출수",
    SUM(CASE WHEN is_click THEN 1 ELSE 0 END) as "클릭수",
    SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as "광고비",
    SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) as "매출",
    SUM(CASE WHEN is_conversion THEN 1 ELSE 0 END) as "전환수",
    CASE WHEN COUNT(*) > 0 
         THEN ROUND(CAST(SUM(CASE WHEN is_click THEN 1 ELSE 0 END) AS DOUBLE) / COUNT(*) * 100, 2)
         ELSE 0 END as "CTR",
    CASE WHEN SUM(CASE WHEN is_click THEN 1 ELSE 0 END) > 0 
         THEN ROUND(SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) / 
                    SUM(CASE WHEN is_click THEN 1 ELSE 0 END), 0)
         ELSE 0 END as "CPC",
    CASE WHEN SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) > 0 
         THEN ROUND(SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) / 
                   (SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END)), 2)
         ELSE 0 END as "ROAS"
FROM ad_combined_log_summary
CROSS JOIN date_params
WHERE 
    date_parse(concat(CAST(year AS VARCHAR), '-', 
                     LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                     LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
    BETWEEN start_date AND end_date
GROUP BY keyword, date_params.start_date, date_params.end_date
HAVING COUNT(*) >= 50  -- 최소 노출수 조건
ORDER BY "매출" DESC
LIMIT 30
```

#### 카테고리별 성과 분석 (기간조회 버전)
```sql
-- 광고 카테고리별 기간 성과 - 카테고리별 효율성 비교
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    ad_category as "광고_카테고리",
    COUNT(*) as "노출수",
    SUM(CASE WHEN is_click THEN 1 ELSE 0 END) as "클릭수",
    SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as "광고비",
    SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) as "매출",
    SUM(CASE WHEN is_conversion THEN 1 ELSE 0 END) as "전환수",
    ROUND(COUNT(*) * 100.0 / SUM(COUNT(*)) OVER(), 1) as "노출_비중%",
    ROUND((SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END)) * 100.0 / 
          SUM(SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END)) OVER(), 1) as "광고비_비중%",
    ROUND(SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) * 100.0 / 
          NULLIF(SUM(SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END)) OVER(), 0), 1) as "매출_비중%",
    CASE WHEN COUNT(*) > 0 
         THEN ROUND(CAST(SUM(CASE WHEN is_click THEN 1 ELSE 0 END) AS DOUBLE) / COUNT(*) * 100, 2)
         ELSE 0 END as "CTR",
    CASE WHEN SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) > 0 
         THEN ROUND(SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) / 
                   (SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END)), 2)
         ELSE 0 END as "ROAS"
FROM ad_combined_log_summary
CROSS JOIN date_params
WHERE 
    date_parse(concat(CAST(year AS VARCHAR), '-', 
                     LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                     LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
    BETWEEN start_date AND end_date
GROUP BY ad_category, date_params.start_date, date_params.end_date
ORDER BY "매출" DESC
```

#### 저성과 광고 리스트 (기간조회 버전)
```sql
-- 저성과 광고 리스트 - CTR이 낮거나 ROAS가 나쁜 광고 파악
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date
),
ad_performance AS (
    SELECT 
        ad_id,
        campaign_id,
        advertiser_id,
        COUNT(*) as impressions,
        SUM(CASE WHEN is_click THEN 1 ELSE 0 END) as clicks,
        SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as cost,
        SUM(CASE WHEN is_conversion THEN conversion_value ELSE 0 END) as revenue,
        SUM(CASE WHEN is_conversion THEN 1 ELSE 0 END) as conversions
    FROM ad_combined_log_summary
    CROSS JOIN date_params
    WHERE 
        date_parse(concat(CAST(year AS VARCHAR), '-', 
                         LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                         LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
        BETWEEN start_date AND end_date
    GROUP BY ad_id, campaign_id, advertiser_id
),
avg_metrics AS (
    SELECT 
        AVG(CASE WHEN impressions > 0 THEN clicks * 100.0 / impressions ELSE 0 END) as avg_ctr,
        AVG(CASE WHEN cost > 0 THEN revenue / cost ELSE 0 END) as avg_roas
    FROM ad_performance
    WHERE impressions >= 100  -- 의미있는 노출수만 포함
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    ap.ad_id as "광고ID",
    CONCAT(SUBSTR(ap.campaign_id, 1, 4), '..', SUBSTR(ap.campaign_id, -4)) as "캠페인ID",
    CONCAT(SUBSTR(ap.advertiser_id, 1, 4), '..', SUBSTR(ap.advertiser_id, -4)) as "광고주ID",
    ap.impressions as "노출수",
    ap.clicks as "클릭수",
    ap.cost as "광고비",
    ap.revenue as "매출",
    ap.conversions as "전환수",
    CASE WHEN ap.impressions > 0 
         THEN ROUND(ap.clicks * 100.0 / ap.impressions, 2)
         ELSE 0 END as "CTR",
    am.avg_ctr as "평균_CTR",
    CASE WHEN ap.cost > 0 
         THEN ROUND(ap.revenue / ap.cost, 2)
         ELSE 0 END as "ROAS",
    am.avg_roas as "평균_ROAS",
    CASE 
        WHEN ap.impressions > 0 AND ap.clicks * 100.0 / ap.impressions < am.avg_ctr * 0.5 
        THEN 'CTR 매우 낮음'
        WHEN ap.cost > 0 AND ap.revenue / ap.cost < am.avg_roas * 0.5 
        THEN 'ROAS 매우 낮음'
        WHEN ap.impressions > 1000 AND ap.clicks = 0 
        THEN '클릭 전무'
        WHEN ap.cost > 100000 AND ap.revenue = 0 
        THEN '전환 전무'
        ELSE '기타'
    END as "문제점"
FROM ad_performance ap
CROSS JOIN avg_metrics am
CROSS JOIN date_params
WHERE 
    (ap.impressions >= 100 AND ap.clicks * 100.0 / ap.impressions < am.avg_ctr * 0.5)
    OR (ap.cost >= 10000 AND ap.revenue / ap.cost < am.avg_roas * 0.5)
    OR (ap.impressions > 1000 AND ap.clicks = 0)
    OR (ap.cost > 100000 AND ap.revenue = 0)
ORDER BY ap.cost DESC
LIMIT 50
```

#### 시간대별 광고주 활동 현황 (기간조회 버전)
```sql
-- 시간대별 활동 광고주 수와 광고비 집중도 분석
WITH date_params AS (
    SELECT 
        DATE('{{ start_date }}') as start_date,
        DATE('{{ end_date }}') as end_date
)
SELECT 
    date_format(start_date, '%Y-%m-%d') || ' ~ ' || date_format(end_date, '%Y-%m-%d') as "조회기간",
    CAST(hour AS INTEGER) as "시간",
    COUNT(DISTINCT advertiser_id) as "활동_광고주수",
    COUNT(DISTINCT campaign_id) as "활동_캠페인수",
    COUNT(DISTINCT ad_id) as "활동_광고수",
    SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END) as "시간별_광고비",
    ROUND((SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END)) * 100.0 / 
          SUM(SUM(cost_per_impression) + SUM(CASE WHEN is_click THEN cost_per_click ELSE 0 END)) OVER(), 1) as "광고비_비중%",
    COUNT(*) as "노출수",
    ROUND(COUNT(*) * 100.0 / SUM(COUNT(*)) OVER(), 1) as "노출_비중%"
FROM ad_combined_log
CROSS JOIN date_params
WHERE 
    date_parse(concat(CAST(year AS VARCHAR), '-', 
                     LPAD(CAST(month AS VARCHAR), 2, '0'), '-', 
                     LPAD(CAST(day AS VARCHAR), 2, '0')), '%Y-%m-%d')
    BETWEEN start_date AND end_date
GROUP BY hour, date_params.start_date, date_params.end_date
ORDER BY CAST(hour AS INTEGER)
```