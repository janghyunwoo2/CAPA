[project]
name = "log-generator"
version = "0.1.0"
description = "Log Generator Simulator"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "faker>=30.0.0",
]

[tool.uv]
python-preference = "only-managed"
