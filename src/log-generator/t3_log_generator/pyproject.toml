[project]
name = "log-generator"
version = "0.2.0"
description = "Ad Log Generator - Production-level Simulator"
readme = "plan.md"
requires-python = ">=3.14"
dependencies = [
    "faker>=30.0.0",
]

[tool.uv]
python-preference = "only-managed"
